## 1. Install dependency

- [x] 1.1 Install `@vercel/speed-insights` as a production dependency

## 2. Add component to root layout

- [x] 2.1 Import `SpeedInsights` from `@vercel/speed-insights/next` in `src/app/layout.tsx`
- [x] 2.2 Render `<SpeedInsights />` alongside `<Analytics />` inside `<body>`

## 3. Verify

- [x] 3.1 Run `next build` to confirm no build errors
